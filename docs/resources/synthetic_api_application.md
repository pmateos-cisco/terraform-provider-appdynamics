---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "appdynamics_synthetic_api_application Resource - terraform-provider-appdynamics"
subcategory: ""
description: |-
  A Synthetic API Monitoring application: a lightweight, account-wide container that appdynamics_synthetic_api_job resources are grouped under. Unlike Synthetic Web Monitoring (which requires a pre-existing Browser RUM app set up via the Controller UI), this container has a full create/delete lifecycle via the API (verified live). There is no update endpoint (verified live), so name forces replacement on change.
---

# appdynamics_synthetic_api_application (Resource)

A Synthetic API Monitoring application: a lightweight, account-wide container that appdynamics_synthetic_api_job resources are grouped under. Unlike Synthetic Web Monitoring (which requires a pre-existing Browser RUM app set up via the Controller UI), this container has a full create/delete lifecycle via the API (verified live). There is no update endpoint (verified live), so name forces replacement on change.

## Example Usage

```terraform
resource "appdynamics_synthetic_api_application" "checkout_api" {
  name = "Checkout API Monitoring"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)

### Read-Only

- `app_key` (String) Server-assigned EUM app key for this application.
- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import appdynamics_synthetic_api_application.checkout_api 10499
```
